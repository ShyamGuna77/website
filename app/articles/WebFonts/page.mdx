

import { ArticleLayout } from "../../components/ArticleLayout";

export const article = {
  author: 'Shyam Guna',
  date: '2025-04-19',
  title: 'My First MDX Article',
  description: 'This is my first article using MDX in Next.js - exploring the possibilities of MDX in a portfolio site.',
};

export const metadata = {
  title: article.title,
  description: article.description,
};

export default (props) => <ArticleLayout article={article} {...props} />;


## 🧠 CSS Grid Mental Model: Think Like a Designer, Code Like a Pro

Most developers approach CSS Grid the wrong way. We jump straight into `grid-template-columns`, `fr` units, or trying to fit items in a rigid matrix. But what if I told you that **Grid is more like a flexible canvas than a spreadsheet**?


